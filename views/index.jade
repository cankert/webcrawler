extends layout

block content
    -var websitelist = websitedata
    //h1= title
    //p Welcome to our test

    // Wrapper
    #wrapper
        // Website LIST

        #websitelist
            div
                h2 Überwachte Websites
                table
                    thead
                        th Website
                        th URL
                        th Delete?
                        tbody
                        each data in websitedata
                            tr
                               td!='<a href=/website/' + data._id + '/' + data.website + '>' + data.website + '</a>'
                               td=data.url
                               td!='<a href="#" class="linkdelwebsite" rel="' + data._id + '"> Delete </a>'
        // /Website LIST

        // ADD USER


        #addWebsite


            div
                p Add Website
                fieldset
                    input#inputWebsiteName(type='text', placeholder='Website')
                    input#inputWebsiteUrl(type='text', placeholder='URL')
                    button#btnAddWebsite Add Website
                    button#btnCrawlAll Crawl All
        #idInfo
            p#userId
        // /ADD USER

    // /WRAPPER
